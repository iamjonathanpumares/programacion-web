var elemento;

// Selecciona todo el documento
elemento = document;
// Selecciona todos los elementos del HTML
elemento = document.all;
// Selecciona el elemento head del HTML
elemento = document.head;
// Selecciona el elemento body del HTML
elemento = document.body;
// Selecciona el dominio actual
elemento = document.domain;
// Selecciona todos los formulario del HTML
elemento = document.forms;
// Selecciona el primer formulario del HTML
elemento = document.forms[0];
// Accediendo a las atributos del formulario
elemento = document.forms[0].id;
elemento = document.forms[0].method;
elemento = document.forms[0].classList;
elemento = document.forms[0].action;
// Selecciona todos los enlaces del HTML
elemento = document.links;
elemento = document.links[0];
elemento = document.links[0].classList;
// Selecciona todos las imagenes del HTML
elemento = document.images;
// Selecciona todos los scripts del HTML
elemento = document.scripts;

console.log(elemento);